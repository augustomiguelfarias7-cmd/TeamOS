# Audio Driver Plan

TeamOS expects audio support from Linux ALSA and userland audio services.

Expected Linux pieces:

- ALSA kernel drivers
- PipeWire
- WirePlumber

TeamOS will later add a sound service for volume, output selection, notification
sound, and assistant audio behavior.
